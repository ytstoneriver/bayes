
[tool.poetry]
name = "sample"
version = "0.1.0"
description = "sample"
authors = ["Kai <muinature@gmail.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
